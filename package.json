{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "generate:github": "NUXT_APP_BASE_URL=/flowchart-ssg-sample/ nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.1.2"
  },
  "dependencies": {
    "@primer/css": "^20.8.1",
    "sass": "^1.58.0"
  }
}
